var limite = 10;
for (var num = 0; num < limite; num++){
    var count = 0
    for(var vezes = 0; vezes < limite; vezes ++){
        if(vezes === 10){
            count = count ++
        }  
    }  
}
console.log(num + " x "+vezes + " = "+ num*vezes);
